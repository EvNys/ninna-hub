// Arquivo mantido para compatibilidade de importação.
// Dados de seed removidos — o painel inicia com a lista vazia.
import { MentorApplication } from '../types';

export const seedApplications: MentorApplication[] = [];
